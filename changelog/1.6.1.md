# v1.6.1

## [1.6.1] - 2026-03-04

### Fixed
- **Google Sign-In Unresponsive:** Patched a critical syntax error in `js/app.js` (literal `\n` characters mistakenly hardcoded on lines 1 and 1695). These syntax errors caused the entire JavaScript payload to fail parsing on load, silently crippling all global event handlers including the Google Sign-In button flow.
