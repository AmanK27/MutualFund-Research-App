# v1.5.3

## [1.5.3] - 2026-03-02

### Fixed
- **Live AMFI Table Parsing:** Patched a strict whitespace RegEx matching bug (`categoryRegex`) that caused the parser to fail entirely in production environments, resulting in 0 loaded funds. The regex now dynamically handles variable spacing before the `(` mapping parenthesis and correctly utilizes the sub-capture group to index category names.
